/**
 * BuildTrack — Sponsor portal
 * Sponsors see only their own requests + progress timeline
 * Phase 1 skeleton — full implementation in Phase 4
 */
export function SponsorPortalPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-3 text-on-surface-variant">
      <span className="material-symbols-outlined text-[48px] opacity-30">person_pin</span>
      <p className="font-label-md text-label-md opacity-50">Sponsor Portal — coming soon</p>
    </div>
  );
}
