/**
 * BuildTrack — Admin panel root
 * Modules, users, SLA, routing, themes, templates, integrations
 * Phase 1 skeleton — full implementation in Phase 3
 */
export function AdminPage() {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-3 text-on-surface-variant">
      <span className="material-symbols-outlined text-[48px] opacity-30">admin_panel_settings</span>
      <p className="font-label-md text-label-md opacity-50">Admin Panel — coming soon</p>
    </div>
  );
}
